<!-- Modal -->
<div id="imageModal" class="modal fade js-loading-bar" role="dialog">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">
                    @Resources.Resource.latitude: <span id="latitudeModal"></span>
                    @Resources.Resource.longitude: <span id="longitudeModal"></span>
                </h4>
                <h4 class="modal-title-loading">
                    <span>@Resources.Resource.loading ...</span>
                </h4>
            </div>
            <div class="modal-body">

            </div>
            <div class="modal-loading">
                <span height="490" class="fa fa-circle-o-notch fa-spin fa-3x text-primary loading-center"></span>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">@Resources.Resource.close</button>
            </div>
        </div>
    </div>
</div>

@*<script>

    var preffix = @Html.Raw(Json.Encode(@ViewBag.urlRootPaFTA));   //Html.Raw(Json.Encode(GlobalConfig.pathFTA));
    var preffixFta = preffix.slice(0, preffix.indexOf("Pa_Acao/NewFTA?"))
    /*
    unitName:"Pimenta Bueno"
    level1Name:"(%) NC Análise Microbiológica em Carcaça"
    level2Name:"Análise Microbiológica em Carcaça"
    level3Name:"Enterobactérias"
    auditorId:"1"
    PercentualNCFTA:100     //Percentual de NC
    ReincidenciaDesvioFTA:1 //Quantidade de NC
    endDate:"27/06/2017"
    startDate:"29/05/2017"
    */
    function RedirecionaParaFTA(r) {
        var url = "";
        url += preffix;
        url += "MetaFTA=" + r.MetaFTA;
        url += "&PercentualNCFTA=" + r.PercentualNCFTA;
        url += "&ReincidenciaDesvioFTA=" + r.ReincidenciaDesvioFTA;
        url += "&_Level1=" + encodeURIComponent(r.level1Name);
        url += "&_Level2=" + encodeURIComponent(r.level2Name);
        url += "&_Level3=" + encodeURIComponent(r.level3Name);
        url += "&Supervisor_Id=" + r.auditorId;
        url += "&Evaluated=" + r.Evaluated;

        if (r.unitId > 0) {
            url += "&Unidade_Id=" + r.unitId;
        } else {
            url += "&Unidade_Id=0"
        }

        url += "&_DataInicioFTA=" + encodeURIComponent(r.startDate);
        url += "&_DataFimFTA=" + encodeURIComponent(r.endDate);
        $('#ftaModal').modal();
        $('#ftaModalSim').attr('href', url);
        //console.log(url);
    }

</script>*@