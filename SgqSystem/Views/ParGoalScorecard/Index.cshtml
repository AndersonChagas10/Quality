@model IEnumerable<Dominio.ParGoalScorecard>

@{
    ViewBag.Title = "Configurar meta scorecard:";
}
<div class="page-content-wrapper" style="background-color:white; padding-left:50px">
    <h2>@ViewBag.Title</h2>

    <p>
        @Html.ActionLink(Resources.Resource.create, "Create")
    </p>
    <table class="table">
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.AddDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.AlterDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IsActive)
                </th>*@
            <th>
                @Html.DisplayName("Limite inferior")
            </th>
            <th>
                @Html.DisplayName("Limite superior")
            </th>
            <th>
                @Html.DisplayName("Data de inicio vigência")
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    @Html.DisplayFor(modelItem => item.AddDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AlterDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsActive)
                </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.PercentValueMid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PercentValueHigh)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InitDate)
                </td>
                <td>
                    @Html.ActionLink(Resources.Resource.edit, "Create", new { id = item.Id }) |
                    @Html.ActionLink(Resources.Resource.details, "Details", new { id = item.Id }) |
                    @Html.ActionLink(Resources.Resource.delete, "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
</div>