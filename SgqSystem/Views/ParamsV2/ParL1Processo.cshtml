<h4>Processos</h4>

<form id="ParL1Processo">

    <br />
    <div class="row" data-form>

        <div class="col-sm-3">

            <input type="hidden" name="ParLevel1_Id" data-id />
            <input type="hidden" name="IsActive" value="true" data-notnull />

            <div class="form-group">
                <label for="ParCluster_Id">Processo:</label>
                <select id="selectL1Cluster" class="input input-lg col-sm-12"
                        name="ParCluster_Id"></select>
            </div>

        </div>

        <div class="col-sm-3">

            <div class="form-group">
                <label for="ParCriticalLevel_Id">Grupo:</label>
                <select id="selectL1CriticalLevel" class="input input-lg col-sm-12"
                        name="ParCriticalLevel_Id"></select>
            </div>

        </div>

        <div class="col-sm-3">

            <div class="form-group">
                <label for="EffectiveDate">Data Vigente:</label>
                <input type="date" class="form-control" id="EffectiveDate" name="EffectiveDate">
            </div>

        </div>

        <div class="col-sm-2">

            <div class="form-group">
                <label for="Points">Pontos:</label>
                <input type="text" class="form-control" id="Points" name="Points">
            </div>

        </div>

        <div class="col-sm-1">

            <div class="form-group" data-action>
                <label></label>
                <button class="form-control btn btn-primary" data-add>+</button>
            </div>

        </div>
    </div>

    <div class="row">
        <div class="col-sm-12">
            <table id="tableParL1Processo" class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th class="col-sm-3">
                            Nome
                        </th>
                        <th class="col-sm-3">
                            Grupo
                        </th>
                        <th class="col-sm-2">
                            Data Vigencia
                        </th>
                        <th class="col-sm-2">
                            Pontos
                        </th>
                        <th class="col-sm-2">

                        </th>
                    </tr>
                </thead>
                <tbody></tbody>
            </table>
        </div>
    </div>
</form>

<script>

    var parL1Processo = {

        //paramtros
        idProcesso: 'ParL1Processo',
        urlSave: '@Url.Action("PostParLevel1XCluster", "api/ParLevel1Api")',

        load: function (parLevel1XCluster) {

            let idProcesso = this.idProcesso;

            apagaCampos(idProcesso);

            $('table#table' + idProcesso + ' tbody').html('');
          
            $(parLevel1XCluster).each(function (i, o) {

                var html = '<tr>';
                html += '<td>' + o.ParCluster.Name + '</td>';
                html += '<td>' + o.ParCriticalLevel.Name + '</td>';
                html += '<td>' + new Date(o.EffectiveDate).toLocaleDateString() + '</td>';
                html += '<td>' + o.Points + '</td>';
                html += '<td><button class="btn btn-sm btn-success" data-table-edit><i class="fa fa-pencil" alt="Editar"></i></button><button class="btn btn-sm btn-danger" data-table-delete alt="Excluir"><i class="fa fa-trash-o"></i></button></td>';
                html += '</tr>';

                $('table#table' + idProcesso + ' tbody').append(html);
                $('table#table' + idProcesso + ' tbody tr:last').data('sgq', o);

            });

            function Save(obj) {

                $.ajax({
                    url: this.urlSave,
                    type: 'POST',
                    dataType: "json",
                    data: JSON.stringify(obj),
                    contentType: "application/json",
                    success: function (data) {
                        openMessageModal("Feito", "Dados Salvos");
                        $('#selectL1').trigger('change');
                        closeLoader();

                    },
                    error: function (request, error) {
                        closeLoader();
                        openMessageModal("Ocorreu um erro", JSON.stringify(request));
                        console.log(error)
                    }
                });
            }

            $('#' + idProcesso).off('click');

            //Adicionar os dados no data-form
            $('#' + idProcesso).on('click', '[data-table-edit]', function (e) {

                var obj = $(this).parents('tr').data('sgq');

                apagaCampos(idProcesso);               
                preencheCampos(idProcesso + ' [data-form]', '', obj);

                $('#' + idProcesso + ' [data-form]').data('sgq', obj);
                $('#' + idProcesso + ' [data-form]').addClass('alert alert-success');
                $('#' + idProcesso + ' [data-form] [data-action] [data-add]').addClass('hide');

                $('#' + idProcesso + ' [data-form] [data-action]').append('<button class="form-control btn btn-success" data-edit>Editar</button>');
                $('#' + idProcesso + ' [data-form] [data-action]').append('<button class="form-control btn btn-danger" data-cancel>x</button>');

            });

            //Adicionar os dados no data-form
            $('#' + idProcesso).on('click', '[data-table-delete]', function (e) {
                var obj = $(this).parents('tr').data('sgq');
                obj.IsActive = false;
                Save(obj);
            });

            $('#' + idProcesso).on('click', '[data-cancel]', function (e) {
                apagaCampos(idProcesso);
            });

            //Salvar o campo editado ou cria um novo
            $('#' + idProcesso).on('click', '[data-edit],[data-add]', function (e) {
                openLoader();
                var obj = $('#' + idProcesso + ' [data-form]').data('sgq');
                var formdata = $('#' + idProcesso + ' [data-form]').serializeFormToObjetc();

                if (!(typeof (obj) == 'undefined'))
                    atualizaValoresDoObjeto(obj, formdata);
                else
                    obj = formdata;

                Save(obj);
            });
        },
    };
</script>