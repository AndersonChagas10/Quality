<h4>Informações Gerais</h4>

<form id="ParL1Geral">

    <br />
    <div class="row">
        <div class="col-sm-6">

            <div class="form-group">
                <label for="Name">Nome:</label>
                <input type="text" class="form-control" id="Name" name="Name">
            </div>

        </div>

        <div class="col-sm-6">

            <div class="form-group">
                <label for="Description">Descrição:</label>
                <input type="text" class="form-control" id="Description" name="Description">
            </div>

        </div>

        <div class="col-sm-6">

            <div class="form-group">
                <label for="ParConsolidationType_Id">Tipo de consolidação:</label>
                <input type="text" class="form-control" id="ParConsolidationType_Id" name="ParConsolidationType_Id">
            </div>

        </div>

        <div class="col-sm-6">

            <div class="form-group">
                <label for="ParFrequency_Id">Frequencia:</label>
                <input type="text" class="form-control" id="ParFrequency_Id" name="ParFrequency_Id">
            </div>

        </div>

        <div class="col-sm-6">

            <div class="form-group">
                <label for="ParScoreType">Tipo scorecard:</label>
                <input type="text" class="form-control" id="ParScoreType" name="ParScoreType">
            </div>

        </div>

        <div class="col-sm-12">

            <button class="btn btn-primary pull-right btn-lg btn-block">Salvar</button>

        </div>
    </div>

</form>

<script>
    @*$("body").on("submit", "form#ParL1Geral", function (event) {
        var form = [];
        $($(this).find('input')).each(function (i, o) {
            form.push(
                {
                    "Identificador": $(o).attr('name'),
                    "MEPropriedadeXGadget_Id": $(o).attr('data-prop-gadget'),
                    "MEParametroXCubo_Id": $(o).attr('data-param-cubo'),
                    "Valore": $(o).val(),
                }
            );
        });

        $.ajax({
            url: '@Url.Action("PostValues", "api/MESketchXGadgets")',
            type: 'Post',
            dataType: "json",
            data: JSON.stringify(form),
            contentType: "application/json",
            success: function (data) {
                alert('cadastro realizado com sucesso');
            },
            error: function (request, error) {
                alert("Request: " + JSON.stringify(request));
            }
        });
        event.preventDefault();
    });*@
</script>